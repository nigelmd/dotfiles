# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
.idea/

_site/
.sass-cache/

# Brainstorming/spec notes — kept locally, never committed to this repo
docs/

# Backups created by mac_install.sh's link_into_home function
**/*.backup-*

# nvim plugin lock — let each machine pin its own plugin versions
.config/nvim/lazy-lock.json
