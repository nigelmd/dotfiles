# set nushell as default
set-option -g default-shell /opt/homebrew/bin/nu

# remap prefix to Control + a
unbind C-b
set -g prefix C-q
bind C-q send-prefix

# navigate splits
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# force a reload of the config file
unbind r
bind r source-file ~/.tmux.conf

set -ga mouse on # enable mouse
set -g base-index 1 # start index from 1
set -g renumber-windows on # renumber windows when closed

# don't allow automatic renaming of windows
# set-option -g allow-rename off

# enable 256 colors
#set -g default-terminal "screen-256color"
# set-option -g default-terminal tmux-256color
set -g default-terminal "tmux-256color"
# This tells Tmux that the xterm-256color terminfo entry supports true color (RGB) capabilities.
# Important when using tmux inside wezterm or any other terminal. You'll notice the difference in neovim in tmux vs outside.
# The background appeared yellow for me without this settings instead of looking grey
set -ag terminal-overrides ",xterm-256color:RGB"

# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'thewtex/tmux-mem-cpu-load'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'xamut/tmux-weather'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'jimeh/tmuxifier'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# settings for tmux-sensible
# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0
# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000
# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5
# Focus events enabled for terminals that support them
set -g focus-events on
# Easier and faster switching between next/prev window
bind C-p previous-window
bind C-n next-window
# If prefix is 'C-a'
bind q last-window

# set status line for tmux to top
set -g status-position top
set -g status-style "bg=default"
set -g status-justify "left"

# pane border look and feel
setw -g pane-border-status top
setw -g pane-border-format ""
setw -g pane-border-lines single
setw -g pane-border-style "bg=default,fg=#{@thm_overlay0}"
setw -g pane-active-border-style "bg=default,fg=#{@thm_red}"

# window look and feel
# I took this from: https://github.com/naivecynics/primary-tmux/blob/main/tmux.conf
# and kept the following format and styling: <window number>: <current command icon> <window name>
# and highlighted the current active window
set -wg automatic-rename on
set -g automatic-rename-format "Window"
set -g window-status-current-style "bg=#{@thm_peach},fg=#{@thm_bg},bold"
set -g window-status-style "bg=#{@thm_bg},fg=#{@thm_rosewater}"
set -g window-status-last-style "bg=#{@thm_bg},fg=#{@thm_peach}"
set -g window-status-activity-style "bg=#{@thm_red},fg=#{@thm_bg}"
set -g window-status-bell-style "bg=#{@thm_red},fg=#{@thm_bg},bold"
set -gF window-status-separator "#[bg=#{@thm_bg},fg=#{@thm_overlay_0}]│"
set -g window-status-format '\
#I: #{?#{==:#{pane_current_command},nu}, ,\
#{?#{==:#{pane_current_command},zsh}, ,\
#{?#{==:#{pane_current_command},fish},󰈺 ,\
#{?#{==:#{pane_current_command},nvim}, ,\
#{?#{==:#{pane_current_command},git},󰊢 ,\
#{?#{==:#{pane_current_command},htop}, ,\
#{?#{==:#{pane_current_command},yazi},󰀶 ,\
#{?#{==:#{pane_current_command},python},󰌠 ,\
#{?#{==:#{pane_current_command},bash}, ,\
#{?#{==:#{pane_current_command},ssh}, ,\
#{?#{==:#{pane_current_command},node}, ,\
#{?#{==:#{pane_current_command},cargo}, ,\
#{?#{==:#{pane_current_command},rustc}, ,\
#{?#{==:#{pane_current_command},lua}, ,\
 󰘧  }}}}}}}}}}}}}}#{?#{!=:#{window_name},Window}, #W,} '
set -g window-status-current-format '\
#I: #{?#{==:#{pane_current_command},nu},  ,\
#{?#{==:#{pane_current_command},zsh}, ,\
#{?#{==:#{pane_current_command},fish},󰈺 ,\
#{?#{==:#{pane_current_command},nvim}, ,\
#{?#{==:#{pane_current_command},git},󰊢 ,\
#{?#{==:#{pane_current_command},htop}, ,\
#{?#{==:#{pane_current_command},yazi},󰀶 ,\
#{?#{==:#{pane_current_command},python},󰌠 ,\
#{?#{==:#{pane_current_command},bash}, ,\
#{?#{==:#{pane_current_command},ssh}, ,\
#{?#{==:#{pane_current_command},node}, ,\
#{?#{==:#{pane_current_command},cargo}, ,\
#{?#{==:#{pane_current_command},rustc}, ,\
#{?#{==:#{pane_current_command},lua}, ,\
 󰘧  }}}}}}}}}}}}}}#{?#{!=:#{window_name},Window}, #W,} '

# Configure Catppuccin
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_status_background "none"
set -g @catppuccin_window_status_style "none"
set -g @catppuccin_pane_status_enabled "off"
set -g @catppuccin_pane_border_status "off"

# this was taken from: https://github.com/89iuv/dotfiles/blob/master/tmux/.tmux.conf
# Configure Cpu
set -g @cpu_percentage_format "%03d%%"
set -g @cpu_low_fg_color "#{E:#{@thm_green}}"
set -g @cpu_medium_fg_color "#{E:#{@thm_yellow}}"
set -g @cpu_high_fg_color "#{E:#{@thm_red}}"
set -g @cpu_medium_thresh "80"
set -g @cpu_high_thresh "90"

# Configure Mem
set -g @ram_percentage_format "%03d%%"
set -g @ram_low_fg_color "#{E:#{@thm_green}}"
set -g @ram_medium_fg_color "#{E:#{@thm_yellow}}"
set -g @ram_high_fg_color "#{E:#{@thm_red}}"
set -g @ram_medium_thresh "80"
set -g @ram_high_thresh "90"
 
# Configure battery
set -g @batt_icon_charge_tier8 ''
set -g @batt_icon_charge_tier7 ''
set -g @batt_icon_charge_tier6 ''
set -g @batt_icon_charge_tier5 ''
set -g @batt_icon_charge_tier4 ''
set -g @batt_icon_charge_tier3 ''
set -g @batt_icon_charge_tier2 ''
set -g @batt_icon_charge_tier1 ''

set -g @batt_icon_status_charged ''
set -g @batt_icon_status_charging ''
set -g @batt_icon_status_discharging '󰂃'
set -g @batt_icon_status_attached ''
set -g @batt_icon_status_unknown ''

set -g @batt_color_charge_primary_tier8 "#{E:#{@thm_mauve}}"
set -g @batt_color_charge_primary_tier7 "#{E:#{@thm_mauve}}"
set -g @batt_color_charge_primary_tier6 "#{E:#{@thm_mauve}}"
set -g @batt_color_charge_primary_tier5 "#{E:#{@thm_mauve}}"
set -g @batt_color_charge_primary_tier4 "#{E:#{@thm_mauve}}"
set -g @batt_color_charge_primary_tier3 "#{E:#{@thm_yellow}}"
set -g @batt_color_charge_primary_tier2 "#{E:#{@thm_yellow}}"
set -g @batt_color_charge_primary_tier1 "#{E:#{@thm_red}}"

set -g @batt_color_status_primary_charged "#{E:#{@thm_mauve}}"
set -g @batt_color_status_primary_charging "#{E:#{@thm_mauve}}"
set -g @batt_color_status_primary_discharging "#{E:#{@thm_red}}"
set -g @batt_color_status_primary_attached "#{E:#{@thm_mauve}}"
set -g @batt_color_status_primary_unknown  "#{E:#{@thm_mauve}}"

# Configure Online
set -g @online_icon "ok"
set -g @offline_icon "nok"

# Configure weather
# set-option -g @tmux-weather-location "Tomsk"
set-option -g @tmux-weather-units "u"
# remove location from weather
# set-option -g @tmux-weather-format "%C+%t+%w+%p+UV:+%u+%l"
set-option -g @tmux-weather-format "%C+%t+%w+%p+UV:+%u"

# status left look and feel
set -g status-left-length 100
set -g status-left ""
set -ga status-left "#{?client_prefix,#{#[bg=#{@thm_red},fg=default,bold]   #S },#{#[bg=default,fg=#{@thm_red},bold]   #S }}"
set -ga status-left "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
set -ga status-left "#[bg=default,fg=#{@thm_green},bold]   #{pane_current_command} "
set -ga status-left "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
# change below to $USER if you're using a POSIX shell. I use nushell so it's $env.USER
set -ga status-left "#[bg=default,fg=#{@thm_blue},bold]   #{=/-32/...:#{s|$env.USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg=default,fg=#{@thm_overlay0},none,bold]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg=default,fg=#{@thm_yellow},bold]#{?window_zoomed_flag,   zoom ,}"

# status right look and feel
set -g status-right-length 100
set -g status-right ""
# set -ga status-right "#[bg=default,fg=#{@thm_blue},bold]  #(tmux-mem-cpu-load -i 2 -m 2 | awk \'{print substr($1, 1, 4)}\')%% "
set -ga status-right "#[fg=#{ram_fg_color}]#{E:#{@custom_status_inline}}  #{E:#{@custom_text}}#{ram_percentage} "
set -ga status-right "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
# set -ga status-right "#[bg=default,fg=#{@thm_green},bold] 󰍛 #{cpu_percentage} "
set -ga status-right "#[fg=#{cpu_fg_color}]#{E:#{@custom_status_inline}}  #{E:#{@custom_text}}#{cpu_percentage} "
set -ga status-right "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
set -ga status-right "#{?#{battery_percentage},#{battery_color_fg}#[bg=default]#{E:#{@custom_status_inline}} #{battery_icon} #{E:#{@custom_text}}#(~/.tmux/plugins/tmux-battery/scripts/battery_percentage.sh | sed 's/%%%%//'| xargs printf %%%%03d)%%%% ,}"
# set -ga status-right "#[bg=default,fg=#{@thm_red},bold] 󱐋#{?#{e|>=:20,#{battery_percentage}},#{#[bg=#{@thm_red},fg=default,bold]},#{#[bg=default,fg=#{@thm_red},bold]}} #{battery_percentage} "
set -ga status-right "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
set -ga status-right "#[bg=#{@thm_bg}]#[fg=#{@thm_yellow}] #{E:@catppuccin_weather_icon}#{weather}"
set -ga status-right "#[bg=default,fg=#{@thm_overlay0},none,bold]│"
set -ga status-right "#[bg=#{@thm_bg},fg=#{@thm_blue}] 󰭦 %Y-%m-%d 󰅐 %I:%M%p"

# By default, tmux `<prefix>` key needs to pressed before `<mykey>` to launch
# sessionx. In case you just want to bind '<mykey>' without the tmux '<prefix>'
# add the following line to turn the prefix off. This option is set to
# on by defaut.
# set -g @sessionx-bind C-o
# set -g @sessionx-prefix off
# set -g @sessionx-fzf-builtin-tmux 'off'

# toggle nushell window with C-/
# if in vim, switch to nu window (create if needed)
# if not in vim (e.g. in the nu window), switch back to last window
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n C-_ if-shell "$is_vim" \
  'run-shell "tmux select-window -t :=nu 2>/dev/null || tmux new-window -n nu"' \
  'last-window'

# sesh
bind-key "T" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' \
    --preview 'sesh preview {}'
)\""

# bootstrap tpm
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# this should always be the last line of tmux
run '~/.tmux/plugins/tpm/tpm'
